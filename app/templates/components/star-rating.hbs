{{#each stars as |star|}}
	<span class="star-rating glyphicon {{if star.full 'glyphicon-star'
	'glyphicon-star-empty'}}"
		{{action "set" star.rating}}>
		{{!-- Define set as the action that occurs on click --}}
	</span>
{{/each}}